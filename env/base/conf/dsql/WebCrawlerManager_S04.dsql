# ------------------------------------------------------------------------------
# WebCrawlerManagerS04
#
# ------------------------------------------------------------------------------

                SELECT
                    A.content_id AS content_id
                FROM
                    tm_content A
                WHERE
${areas}            A.content_areas  = ?
${hostId}       AND A.host_id        = ?
