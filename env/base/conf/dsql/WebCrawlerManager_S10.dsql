# ------------------------------------------------------------------------------
# WebCrawlerManagerS10
# 指定期間に登録されたコンテンツ数を取得する。
# ------------------------------------------------------------------------------

                SELECT
                    count(*) AS cnt
                FROM
                    tm_content 
                WHERE
${fromDate}         create_date >= ?
${toDate}       AND create_date <  ?
