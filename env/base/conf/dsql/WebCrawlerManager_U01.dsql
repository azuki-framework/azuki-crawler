# ------------------------------------------------------------------------------
# WebCrawlerManagerU01
#
# ------------------------------------------------------------------------------

                UPDATE
                    tm_host
                SET
${status}           status           = ?
${date}           , update_date      = ?
${accessDate}     , last_access_date = ?
                WHERE
${id}               host_id = ?
